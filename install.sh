#!/bin/bash

export PATH="$PATH:$(pwd)/target/release/crab-tree"
echo 'export PATH="$PATH:$(pwd)/target/release/crab-tree"' >> ~/.bashrc
echo 'export PATH="$PATH:$(pwd)/target/release/crab-tree"' >> ~/.zshrc

